# Runtime requirements for the deployed demo (Hugging Face Space).
# Local-dev contributors can use the repo-root requirements.txt instead,
# which bundles train-time deps the deployed Space doesn't need.
#
# Pinned to known-good versions. Bump deliberately, never optimistically.

# --- CPU PyTorch (no CUDA wheels — Space disk is 50 GB, full torch is 700 MB) ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1
torchvision==0.19.1

# --- Inference + retrieval ---
ultralytics==8.3.40
faiss-cpu==1.8.0.post1
opencv-python-headless==4.10.0.84
Pillow==10.4.0
numpy==1.26.4
scipy==1.14.1

# --- Web server ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
sse-starlette==2.1.3
python-multipart==0.0.18
pydantic==2.9.2

# --- Hugging Face Hub (artefact download at boot) ---
huggingface_hub[hf_xet]==0.26.5

# --- Config + utilities ---
PyYAML==6.0.2
tqdm==4.67.1

# --- Transitive dependencies pulled in via identification/ modules.
#     These are import-time only (sklearn for AUROC computation in eval scripts,
#     pandas for CSV parsing in analysis helpers, matplotlib because
#     evaluate_embedding loads a module that imports it). Pinning keeps boot
#     time deterministic. If you trim the identification/ imports out of
#     backend/pipeline.py later, these can go too. ---
pandas==2.2.3
matplotlib==3.9.2
scikit-learn==1.5.2
